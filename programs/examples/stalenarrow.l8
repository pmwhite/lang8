tag example;

/* Assignment kills ?*T narrowing. */
main(): int {
    x: int = 1;
    p: ?*int = &x;
    if (p != null) {
        p = null;
        p.*
    }
    0
}

//% test: compile-fail
//% error-contains: "dereferencing optional pointer"
