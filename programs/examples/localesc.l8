tag example;

/* A parameter stored in a global must be @immortal; a stack address is not. */
g: ?*int = null;

stash(p: *int@immortal) {
    g = p
}

main(): int {
    x: int = 0;
    stash(&x);
    0
}

//% test: compile-fail
//% error-contains: "address of local cannot escape"
