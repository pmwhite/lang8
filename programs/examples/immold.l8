tag example;

/* After ) the mark is noregion, not @immortal. */

g: ?*int = null;

stash() @immortal {
    g = new int[1](0);
}

main(): int {
    0;
}

//% test: compile-fail
//% error-contains: "function mark is noregion"
