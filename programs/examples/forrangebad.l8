tag example;

/* Range bounds are int, not []T. */
main(): int {
    xs: []i8 = new i8[1](0i8);
    for i in 0..xs _t: int = i;
    0
}

//% test: compile-fail
//% error-contains: "range end must be int"
