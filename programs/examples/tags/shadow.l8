tag tag_test;

import "lib.l8";

main(): int {
    // A local may shadow a tagged global without bringing its tags into scope.
    tag_count: int = 3;
    TagBox: parser::TagBox = parser::TagBox { value: tag_count };
    if (TagBox.value != 3 || parser_alias::tag_count != 7) {
        return 1;
    }
    write(1, "shadow\n");
    0;
}
