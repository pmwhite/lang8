tag tag_test;

import "lib.l8";

tag parser_alias
tag_copy: TagBox = TagBox { value: tag_count };

main(): int {
    if (parser_alias::tag_copy.value != 7) return 1;
    write(1, "global\n");
    0
}
