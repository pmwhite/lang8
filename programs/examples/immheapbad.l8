tag example;

/* noregion is exact: do not write it if the body does not need it. */
noop() noregion { }

main(): int {
    noop();
    0
}

//% test: compile-fail
//% error-contains: "unnecessary noregion"
