tag example;

/* Dynamic ELF: need a shared library and call an undefined extern through the PLT. */
need "libm.so.6";

extern cosf(value: f32): f32;

putchar(c: i8) {
    write(1, &c, 1);
}

main(): int {
    if (cosf(0.0) == 1.0) putchar('Y');
    putchar('\n');
    0;
}
