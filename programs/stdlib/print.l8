tag std;

// Write one line to standard output.
print(text: str) {
    write(1, text);
    write(1, "\n")
}

// Write one line to standard error.
eprint(text: str) {
    write(2, text);
    write(2, "\n")
}
