/* Taking &n aliases the walk cursor, so s[n] is not proven. */
main(argc: int, argv: [][z]i8): int {
    if (argc < 1) {
        return 1;
    }
    s: [z]i8 = argv[0];
    n: int = 0;
    r: *int = &n;
    r.* = 0;
    s[n];
    0;
}
