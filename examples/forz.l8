/* [z]T is walked, not counted; for requires []T. */

main(argc: int, argv: [][z]i8): int {
    if (argc < 1) { return 1; }
    zs: [z]i8 = argv[0];
    for c in zs {
        _x: i8 = c;
    }
    0;
}
