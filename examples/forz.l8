tag example;

/* str is counted, so iteration never exposes its trailing NUL. */
main(argc: int, argv: []str): int {
    if (argc < 1) {
        return 1;
    }
    zs: str = argv[0];
    for c in zs {
        _x: i8 = c;
    }
    0;
}
