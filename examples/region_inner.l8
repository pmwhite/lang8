/* An inner-region pointer cannot be stored in an outer object. */

type Node = { child: ?*Node; }

main(): int {
    region {
        outer: *Node = new Node { child: null };
        region {
            inner: *Node = new Node { child: null };
            outer.child = inner;
        }
    }
    0;
}
