/* Dynamic ELF: need a shared library and call an undefined extern through the PLT. */
need "libc.so.6";

extern getpid(): int;

putchar(c: i8) {
    write(1, &c, 1);
}

main(): int {
    if (getpid() > 0) {
        putchar('Y');
    }
    putchar('\n');
    0;
}
