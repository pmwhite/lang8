/* Pointers inside a self-bound aggregate must not escape. */

g: ?*int = null;

type Box = { p: *int; };

stash(b: Box@) {
    g = b.p;
}

main(): int {
    x: int = 0;
    stash(Box { p: &x });
    0;
}
