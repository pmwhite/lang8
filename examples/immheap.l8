/* @immortal is exact and transitive: stash stashes new, so main must say so too. */

g: ?*int = null;

stash() @immortal {
    g = new int[1](0);
}

main() @immortal: int {
    stash();
    write(1, "Y\n", 2);
    0;
}
