/* A region pointer cannot be stored in a local that outlives the region. */

type Cell = { v: int; }

main(): int {
    p: ?*Cell = null;
    region {
        q: *Cell = new Cell { v: 1 };
        p = q;
    }
    0;
}
