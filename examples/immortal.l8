/* @immortal is process lifetime: the pointer may be stored in a global. */

g: ?*int = null;

stash(p: *int@immortal) {
    g = p;
}

main() @immortal: int {
    heap: []int = new int[1](3);
    stash(&heap[0]);
    q: ?*int = g;
    if (q != null) {
        if (q.* == 3) {
            write(1, "Y\n", 2);
        }
    }
    0;
}
