/* noreturn functions must diverge; exit is an extern. */
extern exit(code: int): noreturn;

die(code: int): noreturn {
    exit(code);
}

greet(): noreturn {
    write(1, "Hi\n", 3);
    die(0);
}

main(): int {
    greet();
}
