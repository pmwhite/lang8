/* noreturn functions must diverge; exit is a noreturn builtin. */
die(code: int): noreturn {
    exit(code);
}

greet(): noreturn {
    write(1, "Hi\n", 3);
    die(0);
}

main(): int {
    greet();
}
